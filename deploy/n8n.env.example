# Copy of ~/n8n-app/.env with these overrides. Real file is deploy/n8n.env (gitignored).
N8N_LISTEN_ADDRESS=0.0.0.0
N8N_PORT=5678
NODE_FUNCTION_ALLOW_BUILTIN=crypto
# Redis credential host inside the stack is "redis"; the Claude shim on the host is host.docker.internal:<port>
